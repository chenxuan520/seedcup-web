# SeedCup Web 项目与工程规则

## 项目背景

本仓库是 2023 年种子杯（SeedCup 2023）炸弹人赛题的浏览器交互实现。页面既是
可玩的演示，也是 C++ 服务端、规则机器人和神经网络模型的 Web 复刻验证平台。

相关权威仓库：

- 官方 C++ 服务端：<https://github.com/chenxuan520/seedcup2023>
- C++ Bot SDK：<https://gitee.com/chenxuan520/seedcup-cppsdk>
- C++ 深度学习库：<https://github.com/chenxuan520/deeplearning>
- 当前 Web 仓库：<https://github.com/chenxuan520/seedcup-web>

本项目最重要的工程约束是“与 C++ 一致”。服务端规则以 `seedcup2023` 为权威，
规则机器人以指定的 C++ Bot 版本为权威，神经网络推理以 C++ 模型和特征提取器
为权威。不要因为 Web 端看起来更安全、更合理或更容易实现而擅自改变算法。

## 用户体验目标

首屏必须是可直接运行的机器人竞技场，不是营销页。默认蓝方是简单机器人，红方
是纯神经网络，点击“开始”后双方应自动连续行动。用户也可以切换 2 至 4 名玩家、
11/13/15/17 地图、六种控制方式和高级规则参数。

关键交互包括：

- 开始、暂停、继续、单步、重开和随机地图。
- 固定 seed 复现。
- 玩家数量、地图大小和机器人类型切换。
- 爆炸时间、随机延迟、初始速度、炸弹、火力、生命、泥墙、道具、随机墙和最大
  回合设置。
- WASD、方向键、空格和屏幕方向键手动控制。
- 对局 JSON 导出与导入。
- 中文玩法帮助、道具说明、状态显示和爆炸效果。

上述功能任何一项都不能只做静态显示；必须有浏览器功能测试覆盖真实操作。

## 机器人类型

Web 端提供六种控制方式：

- `manual`：手动玩家。
- `easy`：比赛版 C++ easy Bot；每回合固定最多一个动作。
- `hard`：比赛提交 `c7cdf9e` 的 C++ hard 规则 Bot；按速度提交多个动作，
  每个独立客户端使用自己的 glibc 随机方向序。
- `search`：规则评估加浅层搜索。
- `nn`：纯 RNN 神经网络策略。
- `hybrid`：搜索为主、神经网络作为小权重先验。

easy 的唯一权威实现是 C++ SDK 提交
`c7cdf9ee3ede1c7699ec7e6a9a72b1791b62f7af:src/bot.h`，文件 SHA-256 为
`ec37e5f86846108fe1028936192d0dd9773c8d7887a915792f7577cc1a7b624c`。
不能拿训练分支后来加入“更安全”逻辑的 easy 当作比赛版。

纯神经网络和混合搜索共用 `public/models/pure-nn.rnn`。模型是 C++ 训练库生成的
`DLRNNH1` 文本格式，不是 JSON。浏览器直接下载并解析，不依赖推理服务。

## 运行与并发模型

浏览器主线程模拟 C++ 服务端。每个非手动机器人运行在独立 Web Worker 中：

1. 每回合开始时，主线程向所有 Worker 发送同一个只读状态快照。
2. Worker 独立保留规则 Bot 状态、随机数状态或 RNN hidden/history。
3. Worker 计算该客户端的完整动作数组。
4. 主线程按照 Worker 实际返回顺序逐批执行动作。
5. 所有客户端返回或达到回合截止时间后，统一移动炸弹、结算爆炸和推进回合。

这对应原系统“多个 C++ 客户端并发计算，服务端单线程按网络到达顺序处理动作，
定时器统一 FlushTime”。不要把机器人合并到共享状态中，也不要按固定玩家顺序
等待所有 Worker 后再统一提交。

## 目录与关键文件

- `src/engine.ts`：地图生成、服务端规则、动作执行、爆炸、道具、胜负、序列化。
- `src/bots.ts`：easy、hard、搜索、纯 NN 和混合机器人控制器。
- `src/rnn.ts`：`DLRNNH1` 解析、特征提取、RNN 前向、hidden/history 状态。
- `src/cpp-random.ts`：兼容 C++ `mt19937`、`mt19937_64` 和 glibc `rand()`。
- `src/cpp-game-sim.ts`：搜索 rollout 使用的 C++ `GameSim` 对齐模拟器。
- `src/bot-worker.ts`：独立机器人 Worker 生命周期和整批动作计算。
- `src/main.ts`：页面、控制器、Worker 调度、自动播放、导入导出和 Canvas 渲染。
- `src/app.css`：桌面、窄屏和移动端布局。
- `fixtures/`：由 C++ 导出的固定对拍数据。
- `tools/cpp-fixtures/`：生成 C++ fixture 和长局 replay 的工具。
- `scripts/check-*.ts`：C++/TypeScript 一致性与专项回归。
- `scripts/e2e.ts`：完整浏览器功能回归。
- `.github/workflows/`：测试、覆盖率和 Pages 部署门禁。

## 权威对齐规则

- 地图、炸弹、碰撞、道具、计分和胜负以官方 C++ 服务端为准。
- easy 的 RNG 使用独立客户端进程语义下的 glibc `rand()`，每个客户端初始 seed
  为 1；`std::random_shuffle` 的随机数消费顺序不能改变。
- C++ SDK 的 `GameMsg.players` 来源于网格扫描和 GCC 8 `unordered_map` 迭代顺序，
  多人局不能直接使用 JavaScript 玩家数组顺序。
- easy 即使吃到加速，每回合仍固定一个动作；hard 和其他 Bot 可按 speed 多步。
- Worker 必须保留独立 RNG/RNN 状态，重开或切换机器人时重新初始化。
- 模型特征维度、动作历史、结果特征、动作上下文和 RNN hidden state 必须与 C++
  数值对拍。
- 当前默认道具权重是 `12:12:4:1:4:6:6`，对应火力、炸弹、回血、无敌、护盾、
  加速和手套；无敌在已生成道具中的理论比例为 `1/45`。

## 已知高风险区域

- `loadPureNnPolicy()` 是异步的。模型加载完成或失败不能重置并中断用户已经开始的
  对局。
- “单步可用”不等于“开始按钮可用”。自动播放定时器、暂停、继续必须单独测试。
- `resetGame()` 同时处理状态、Worker、按钮和计时器，是竞态高发点。
- 导入存档后必须同步服务端 ID 计数器、RNG、Worker 和机器人内部状态。
- Canvas 动画每帧执行，不能在热路径反复解析模型、重建 Worker 或做昂贵 DOM
  查询。
- 四人局会暴露二人局没有的 `unordered_map` 顺序、同格碰撞和并发到达问题。
- 不要为“防自炸”或“更聪明”修改比赛版 easy；如果原 C++ 在同一状态输出相同
  动作，那就是 Web 应保留的行为。

## 开发命令

```bash
npm ci
npm run dev -- --host 0.0.0.0
npm run build
npm run test:all
npm run test:browser
```

最终 CI 命令和覆盖率命令以 `package.json` 为准。新增功能时必须同步更新测试矩阵
和本文件。

## 提交前独立审查

任何代码修改在提交前都必须启动一个独立的 review agent。实现者不能同时充当
最终 reviewer。

Reviewer 至少检查以下内容：

- 功能是否符合当前用户需求，是否改变了未要求修改的游戏规则或机器人行为。
- 自动播放、单步、暂停、继续、重开、设置、导入导出和多人对局是否存在回归。
- Worker、Promise、定时器、模型加载和重置流程是否存在竞态或状态分叉。
- C++ fixture、TypeScript 实现和浏览器运行结果是否仍然一致。
- 单元测试、回归测试和浏览器功能测试是否覆盖本次修改及其失败路径。
- 生产代码覆盖率的 statements、branches、functions、lines 是否全部不低于 90%。
- Git 暂存范围是否只包含本次任务需要的文件，是否误带模型、截图或用户改动。
- README、AGENTS.md、命令和实际实现是否一致，新的 agent 是否能仅依赖仓库文档
  完成构建、测试和安全修改。

如果 reviewer 发现任何阻塞问题，必须先修复并重新运行完整测试，再由独立
reviewer 重新审查。只有 reviewer 明确给出“无阻塞问题”结论后才允许提交。

## 测试门禁

每次提交前必须执行仓库定义的完整测试命令，至少包括：

- TypeScript 类型检查和生产构建。
- 游戏规则、随机数、道具、机器人和模型推理单元测试。
- C++/TypeScript fixture 对拍和多人回归测试。
- Playwright 浏览器功能测试。
- 真实点击“开始”后的自动回合推进、暂停、继续和重开。
- 模型仍在加载时提前点击“开始”的竞态测试。
- 桌面和移动端布局、六种机器人、2 至 4 人、地图尺寸、设置、帮助和导入导出。

GitHub Actions 必须运行同一套测试。Pages 部署只能依赖测试成功的构建产物，
不能绕过测试直接重新构建和发布。

## 覆盖率要求

所有生产 `src` TypeScript 代码都纳入覆盖率统计。测试脚本、fixture、生成物和
第三方文件不计入生产代码覆盖率。

以下四项全局覆盖率必须分别不低于 90%：

- statements
- branches
- functions
- lines

不得通过排除核心文件、空实现、无意义调用或降低阈值来通过门禁。新增或修改的
功能必须同时覆盖成功路径、失败路径和关键边界条件。

## 上线验证

推送后必须等待所有 GitHub Actions 完成。Pages 发布成功后还要对线上 URL 执行
浏览器功能回归，至少确认：

- 点击“开始”后回合自动连续推进且机器人实际移动。
- 暂停后回合停止，继续后恢复推进。
- 页面无 `pageerror` 和控制台错误。
- 当前线上资源哈希对应本次提交。

未完成上述验证时，不得宣称任务已经完成。

## Git 与文件安全

- 工作树可能包含用户修改。不得 reset、checkout 或覆盖无关改动。
- 手工编辑使用补丁，避免批量重写无关文件。
- 不提交临时截图、coverage 原始数据、构建目录或训练中间模型。
- 提交前检查 `git status`、`git diff --check` 和暂存文件清单。
- Commit 作者使用仓库现有用户配置，不擅自修改用户身份。
- 推送后检查远端 commit SHA 和所有 Actions 最终状态。
